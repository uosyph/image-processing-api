// import sharp from 'sharp';
// import resize from '../routes/api/resize';

import sharp from "sharp";


function resize(filename: string, height: number, width: number) {
    // sharp 
}

export default resize;
